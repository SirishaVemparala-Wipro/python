# Asking for user input
price = float(input("Enter the price of the product: "))

# Formatting the price to two decimal places
output = "The price of the product is ${:.2f}.".format(price)

# Displaying the output
print(output)
